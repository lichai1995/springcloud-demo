spring:
  application:
    name: microservice-provider-user
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    # 自定义的元数据
    metadata-map:
      my-metadata: 自定义元数据
server:
  port: 8081