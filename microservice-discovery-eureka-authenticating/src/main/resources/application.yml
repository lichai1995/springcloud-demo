security:
  basic:
    enabled: true  # 开启基于HTTP basic的认证
  user:
    name: admin  # 配置eureka登录的账号
    password: admin # 配置eureka登录的密码
server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/